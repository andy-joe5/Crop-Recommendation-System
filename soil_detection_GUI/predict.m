global y;

if y==34
    set(handles.edit1,'String','Alluvial soil');
    set(handles.edit3,'String','surface water');
    set(handles.edit4,'String','remove sediments');
    h=msgbox('ALLUVIAL SOIL');
    defaultString = 'Alluvial soils remove sediments and nutrients flowing in the adjacent water';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end 

if y==20
    set(handles.edit1,'String','Alluvial soil');
    set(handles.edit3,'String','surface water');
    set(handles.edit4,'String','remove sediments');
    h=msgbox('ALLUVIAL SOIL');
    defaultString = 'Alluvial soils remove sediments and nutrients flowing in the adjacent water';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end 
if y==30
    set(handles.edit1,'String','Alluvial soil');
    set(handles.edit3,'String','surface water');
    set(handles.edit4,'String','remove sediments');
    h=msgbox('ALLUVIAL SOIL');
    defaultString = 'Alluvial soils remove sediments and nutrients flowing in the adjacent water';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end 
if y==6
    set(handles.edit1,'String','Alluvial soil');
    set(handles.edit3,'String','surface water');
    set(handles.edit4,'String','remove sediments');
    h=msgbox('ALLUVIAL SOIL');
    defaultString = 'Alluvial soils remove sediments and nutrients flowing in the adjacent water';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end 

if y==4
    set(handles.edit1,'String','Alluvial soil');
    set(handles.edit3,'String','surface water');
    set(handles.edit4,'String','remove sediments');
    h=msgbox('ALLUVIAL SOIL');
    defaultString = 'Alluvial soils remove sediments and nutrients flowing in the adjacent water';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end 
if y==20
    set(handles.edit1,'String','Alluvial soil');
    set(handles.edit3,'String','surface water');
    set(handles.edit4,'String','remove sediments');
    h=msgbox('ALLUVIAL SOIL');
    defaultString = 'Alluvial soils remove sediments and nutrients flowing in the adjacent water';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end 

if y==11
    set(handles.edit1,'String','Alluvial soil');
    set(handles.edit3,'String','surface water');
    set(handles.edit4,'String','remove sediments');
    h=msgbox('ALLUVIAL SOIL ');
    defaultString = 'Alluvial soils remove sediments and nutrients flowing in the adjacent water';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end 
if y==17
    set(handles.edit1,'String','Alluvial soil');
    set(handles.edit3,'String','surface water');
    set(handles.edit4,'String','remove sediments');
    h=msgbox('ALLUVIAL SOIL ');
    defaultString = 'Alluvial soils remove sediments and nutrients flowing in the adjacent water';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end 
if y==13
    set(handles.edit1,'String','Alluvial soil');
    set(handles.edit3,'String','surface water');
    set(handles.edit4,'String','remove sediments');
    h=msgbox('ALLUVIAL SOIL ');
    defaultString = 'Alluvial soils remove sediments and nutrients flowing in the adjacent water';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end 
if y==5
    set(handles.edit1,'String','Clay soil');
    set(handles.edit3,'String','feel very sticky');
    set(handles.edit4,'String','fine-grained natural soil');
    h=msgbox('CLAY SOIL');
    defaultString = 'Clays develop plasticity when wet, due to a molecular film of water';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end 
if y==2
    set(handles.edit1,'String','Clay soil');
    set(handles.edit3,'String','feel very sticky');
    set(handles.edit4,'String','fine-grained natural soil');
    h=msgbox('CLAY SOIL');
    defaultString = 'Clays develop plasticity when wet, due to a molecular film of water';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end 
if y==7
    set(handles.edit1,'String','Clay soil');
    set(handles.edit3,'String','feel very sticky');
    set(handles.edit4,'String','fine-grained natural soil');
    h=msgbox('CLAY SOIL');
    defaultString = 'Clays develop plasticity when wet, due to a molecular film of water';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end 
if y==3
    set(handles.edit1,'String','Clay soil');
    set(handles.edit3,'String','feel very sticky');
    set(handles.edit4,'String','fine-grained natural soil');
    h=msgbox('CLAY SOIL');
    defaultString = 'Clays develop plasticity when wet, due to a molecular film of water';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end 
% if y==6
%     set(handles.edit1,'String','Clay soil');
%     set(handles.edit3,'String','feel very sticky');
%     set(handles.edit4,'String','fine-grained natural soil');
%     h=msgbox('CLAY SOIL');
%     defaultString = 'Clays develop plasticity when wet, due to a molecular film of water';
%     NET.addAssembly('System.Speech');
%     obj = System.Speech.Synthesis.SpeechSynthesizer;
%     obj.Volume = 100;
%     Speak(obj, defaultString);
% 
% end 
if y==9
    set(handles.edit1,'String','Clay soil');
    set(handles.edit3,'String','feel very sticky');
    set(handles.edit4,'String','fine-grained natural soil');
    h=msgbox('CLAY SOIL');
    defaultString = 'Clays develop plasticity when wet, due to a molecular film of water';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end 

if y==16
    set(handles.edit1,'String','Gravel  soil');
    set(handles.edit3,'String','rock fragments');
    set(handles.edit4,'String','crushed stone');
    h=msgbox('GRAVEL');
    defaultString = 'sedimentary and erosive geologic processes';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end 
if y==29
    set(handles.edit1,'String','Gravel  soil');
    set(handles.edit3,'String','rock fragments');
    set(handles.edit4,'String','crushed stone');
    h=msgbox('GRAVEL');
    defaultString = 'sedimentary and erosive geologic processes';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end 
if y==27
    set(handles.edit1,'String','Gravel  soil');
    set(handles.edit3,'String','rock fragments');
    set(handles.edit4,'String','crushed stone');
    h=msgbox('GRAVEL');
    defaultString = 'sedimentary and erosive geologic processes';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end
if y==21
    set(handles.edit1,'String','Gravel  soil');
    set(handles.edit3,'String','rock fragments');
    set(handles.edit4,'String','crushed stone');
    h=msgbox('GRAVEL');
    defaultString = 'sedimentary and erosive geologic processes';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end
if y==42
    set(handles.edit1,'String','Gravel  soil');
    set(handles.edit3,'String','rock fragments');
    set(handles.edit4,'String','crushed stone');
    h=msgbox('GRAVEL');
    defaultString = 'sedimentary and erosive geologic processes';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end
if y==24
    set(handles.edit1,'String','Gravel  soil');
    set(handles.edit3,'String','rock fragments');
    set(handles.edit4,'String','crushed stone');
    h=msgbox('GRAVEL');
    defaultString = 'sedimentary and erosive geologic processes';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end
if y==32
    set(handles.edit1,'String','Gravel  soil');
    set(handles.edit3,'String','rock fragments');
    set(handles.edit4,'String','crushed stone');
    h=msgbox('GRAVEL');
    defaultString = 'sedimentary and erosive geologic processes';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end
if y==10
    set(handles.edit1,'String','Gravel  soil');
    set(handles.edit3,'String','rock fragments');
    set(handles.edit4,'String','crushed stone');
    h=msgbox('GRAVEL');
    defaultString = 'sedimentary and erosive geologic processes';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end
if y==10
    set(handles.edit1,'String','Red soil');
    set(handles.edit3,'String','Thin organic and organic-mineral layers');
    set(handles.edit4,'String','13% of Earth');
    h=msgbox('RED SOIL');
    defaultString = 'Red soil is a type of soil that typically develops in warm, temperate, and humid climates and comprise approximately 13% of Earth';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end
if y==23
    set(handles.edit1,'String','Red soil');
    set(handles.edit3,'String','Thin organic and organic-mineral layers');
    set(handles.edit4,'String','13% of Earth');
    h=msgbox('RED SOIL');
    defaultString = 'Red soil is a type of soil that typically develops in warm, temperate, and humid climates and comprise approximately 13% of Earth';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end
if y==14
    set(handles.edit1,'String','Red soil');
    set(handles.edit3,'String','Thin organic and organic-mineral layers');
    set(handles.edit4,'String','13% of Earth');
    h=msgbox('RED SOIL');
    defaultString = 'Red soil is a type of soil that typically develops in warm, temperate, and humid climates and comprise approximately 13% of Earth';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end
if y==38
    set(handles.edit1,'String','Red soil');
    set(handles.edit3,'String','Thin organic and organic-mineral layers');
    set(handles.edit4,'String','13% of Earth');
    h=msgbox('RED SOIL');
    defaultString = 'Red soil is a type of soil that typically develops in warm, temperate, and humid climates and comprise approximately 13% of Earth';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end
if y==19
    set(handles.edit1,'String','SAND');
    set(handles.edit3,'String','coarse textured');
    set(handles.edit4,'String','few nutrients');
    h=msgbox('SAND');
    defaultString = 'Sandy soils are those that are generally coarse textured until 50 cm depth';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end
if y==28
    set(handles.edit1,'String','SAND');
    set(handles.edit3,'String','coarse textured');
    set(handles.edit4,'String','few nutrients');
    h=msgbox('SAND');
    defaultString = 'Sandy soils are those that are generally coarse textured until 50 cm depth';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end
if y==18
    set(handles.edit1,'String','SAND');
    set(handles.edit3,'String','coarse textured');
    set(handles.edit4,'String','few nutrients');
    h=msgbox('SAND');
    defaultString = 'Sandy soils are those that are generally coarse textured until 50 cm depth';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end
if y==20
    set(handles.edit1,'String','SAND');
    set(handles.edit3,'String','coarse textured');
    set(handles.edit4,'String','few nutrients');
    h=msgbox('SAND');
    defaultString = 'Sandy soils are those that are generally coarse textured until 50 cm depth';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end
if y==28
    set(handles.edit1,'String','SAND');
    set(handles.edit3,'String','coarse textured');
    set(handles.edit4,'String','few nutrients');
    h=msgbox('SAND');
    defaultString = 'Sandy soils are those that are generally coarse textured until 50 cm depth';
    NET.addAssembly('System.Speech');
    obj = System.Speech.Synthesis.SpeechSynthesizer;
    obj.Volume = 100;
    Speak(obj, defaultString);

end